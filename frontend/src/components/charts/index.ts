/**
 * Chart primitives — a11y wrapper for canvas charts.
 *
 * Populated per specs/012-design-graphics-uplift/contracts/chart-treatment.md
 * (canvas accessibility: role="img" + sr-only data table fallback).
 */
export { ChartFrame } from './ChartFrame';
export type { ChartFrameProps, ChartTable } from './ChartFrame';
