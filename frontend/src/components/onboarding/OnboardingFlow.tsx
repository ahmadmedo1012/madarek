import type { ReactElement } from 'react';

/**
 * Onboarding flow shell — implemented in T120..T124.
 *
 * Until then, this stub exports an empty component so that
 * imports from feature work-in-progress files do not break the
 * type-check.
 */
export function OnboardingFlow(_props: { isReplay?: boolean }): ReactElement | null {
  return null;
}
