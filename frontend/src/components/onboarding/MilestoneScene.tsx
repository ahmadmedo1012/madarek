import type { ReactElement } from 'react';

/**
 * Milestone scene presenter — implemented in T127..T128.
 *
 * Until then, this stub exports an empty component so that
 * imports from feature work-in-progress files do not break the
 * type-check.
 */
export function MilestoneScene(_props: { id: string }): ReactElement | null {
  return null;
}
