/**
 * Theme module — `GET/PUT /api/v1/me/theme`.
 *
 * Implemented in T021..T023 per
 * specs/012-design-graphics-uplift/contracts/theme-state.md.
 *
 * This stub holds the router export shape so wiring tasks have a
 * stable import target.
 */
import { Router } from 'express';

export const themeRouter = Router();
