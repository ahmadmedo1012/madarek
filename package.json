{
  "name": "madarek",
  "version": "1.0.0",
  "private": true,
  "description": "Madarek AI — Arabic-RTL educational platform for the University of Zawia.",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "build": "npm run build -w frontend && npm run build -w backend && npm run db:deploy",
    "start": "node backend/dist/index.js",
    "dev": "npm run dev -w backend",
    "dev:web": "npm run dev -w frontend",
    "db:migrate": "npm run prisma:migrate -w backend",
    "db:deploy": "npm run prisma:deploy -w backend",
    "db:seed": "npm run prisma:seed -w backend"
  }
}
