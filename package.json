{
  "name": "madarek",
  "version": "1.0.0",
  "private": true,
  "description": "Madarek AI — production-grade educational platform for the University of Zawia.",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:backend dev:frontend",
    "dev:backend": "npm --workspace backend run dev",
    "dev:frontend": "npm --workspace frontend run dev",
    "build": "npm run build:frontend && npm run build:backend && npm run db:deploy",
    "build:frontend": "npm --workspace frontend run build",
    "build:backend": "npm --workspace backend run build",
    "start": "node backend/dist/index.js",
    "lint": "npm --workspace backend run lint && npm --workspace frontend run lint",
    "typecheck": "npm --workspace backend run typecheck && npm --workspace frontend run typecheck",
    "test": "npm --workspace backend run test && npm --workspace frontend run test",
    "db:generate": "npm --workspace backend run prisma:generate",
    "db:migrate": "npm --workspace backend run prisma:migrate",
    "db:deploy": "npm --workspace backend run prisma:deploy",
    "db:seed": "npm --workspace backend run prisma:seed",
    "db:studio": "npm --workspace backend run prisma:studio",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md,css}\""
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "prettier": "^3.2.5"
  }
}
